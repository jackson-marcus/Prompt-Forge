services:
  api:
    build: .
    env_file: .env
    ports:
      - "8460:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      PROMPTFORGE_API_URL: http://api:8000
    ports:
      - "8961:8501"
    depends_on:
      - api
